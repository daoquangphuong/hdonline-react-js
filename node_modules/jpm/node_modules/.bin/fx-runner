#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fx-runner/bin/fx-runner" "$@"
  ret=$?
else 
  node  "$basedir/../fx-runner/bin/fx-runner" "$@"
  ret=$?
fi
exit $ret
